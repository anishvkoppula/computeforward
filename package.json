{
  "name": "computeforward",
  "version": "1.0.0",
  "description": "ComputeForward — Education Platform. Static frontend + lightweight application backend.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
